{
  "name": "@aeona/rest",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": " tsc --project tsconfig.json --noEmit",
    "start": "node --es-module-specifier-resolution=node --no-warnings .",
    "dev": "node --es-module-specifier-resolution=node --no-warnings -r dotenv/config .",
    "test": "tsc --noEmit -p tsconfig.build.json && eslint ."
  },
  "dependencies": {
    "net-ipc": "^2.0.1"
  },
  "license": "MIT"
}
